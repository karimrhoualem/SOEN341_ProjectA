Run the .java files located in the 'programs' package from the command line by passing them one of the files in the 'TestFiles' directory. 

This is where the main methods are found. 

The 'administrator_template', 'command_line_strategy', and 'factory' packages are imported into the 'programs' files.